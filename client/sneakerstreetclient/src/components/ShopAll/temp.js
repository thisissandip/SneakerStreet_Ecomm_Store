/* 	const [allbrands, Setallbrands] = useState([]);
	const [allcategories, Setallcategories] = useState([]);
    const [allgenders, Setallgenders] = useState([]); */

/* 			allproducts.forEach((item) => {
				if (!allcategories.includes(item.Details.Category)) {
					// this means does not include
					allcategories.push(item.Details.Category);
				}
				if (!allbrands.includes(item.Details.Brand)) {
					allbrands.push(item.Details.Brand);
				}

				if (!allgenders.includes(item.Details.Gender[0])) {
					allgenders.push(item.Details.Gender[0]);
				}
			}); */

/* 	const allbrandsDIV = allbrands.map((brand, i) => (
		<div key={i} className='input-div'>
			<input id={brand} type='checkbox' />
			<label htmlFor={`#${brand}`}>{brand}</label>
		</div>
	));

	const allgendersDIV = allgenders.map((brand, i) => (
		<div key={i} className='input-div'>
			<input id={brand} type='checkbox' />
			<label htmlFor={`#${brand}`}>{brand}</label>
		</div>
	));

	const allcatDIV = allcategories.map((brand, i) => (
		<div key={i} className='input-div'>
			<input id={brand} type='checkbox' />
			<label htmlFor={`#${brand}`}>{brand}</label>
		</div>
	)); */

{
	/* 			<div className='filters-container'>
					<div className='filter-cont-title'>Filters </div>
					<div className='single-filter-container'>
						<div className='filter-title'>Brands</div>
						{allbrandsDIV}
					</div>
					<div className='single-filter-container'>
						<div className='filter-title'>Gender</div>
						{allgendersDIV}
					</div>
					<div className='single-filter-container'>
						<div className='filter-title'>Categories</div>
						{allcatDIV}
					</div>
				</div> */
}
